name: scan_qr_app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"  # <= cocok dengan Dart SDK kamu (3.7.2)

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^3.5.6
  permission_handler: ^11.0.1
  path_provider: ^2.1.1
  url_launcher: ^6.3.1
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true